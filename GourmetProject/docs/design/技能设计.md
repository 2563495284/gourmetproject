# 技能

## 实现架构：具体子技能库 + 有序引用 + 自动描述

技能配置分两层（表结构见 [`docs/配置表说明.md`](../配置表说明.md) 5.1 节）：

- **具体子技能 `sub_skill`**：一行 = 某个技能的一条独占完整效果，含全部 `SkillRuleDef` 字段（trigger/条件/行为/数值/参数）+ `signed/descTemplate`。ID 为 `<skillId>_<从 1 开始的序号>`；不跨技能复用，内容相同也分别写行。
- **技能 `skill`**：`id/termId/subSkills`；`subSkills` 正向、有序引用子技能 id（`|` 分隔），执行顺序即列表下标。描述由所引用子技能的占位符模板**按序自动拼接**。展示标题按 `termId` 术语名（无则空）。

要点：

- **一个食物一个 skill**：每个食物的 `dish_base.skills` 只引用一个技能 `sk_<食物id>`，该食物的所有效果（计分/被动/甜蜜传递…）都是这个技能的子技能。数据脚本以「多个 skill 片段」的作者友好写法编写，`build_merged_skills()` 在输出阶段按菜合并成单一技能。
- 每个技能按 `subSkills` 顺序维护自己的子技能行。例如 `sk_cookie` 的第一条效果是 `sk_cookie_1`，`sk_macaron` 的两条效果依次是 `sk_macaron_1`、`sk_macaron_2`。相同效果出现在不同技能时也分别配置，当前 48 个技能共对应 76 条独占子技能。
- **甜蜜传递=子技能**：甜蜜传递作为某菜技能的一个子技能，运行时把「本 skill 内的其他子技能」作为外来子技能传给目标，目标在本场经营挑战结算时随自身一并施加（作用域相对目标计算），一次食物生效一次、食物结束清除。传递子技能本身被排除、不会链式传递。详见 [`技能.md`](技能.md) §5.2 与 [`美味值结算.md`](美味值结算.md)。
- 运行时：适配层 `GameplayContentBuilder` 遍历 `subSkills`，把每个具体子技能合成为现有的 `SkillRuleDef`（`order`=下标）；结算逻辑（`SkillConditionEvaluator`/`SkillRuleEffectSource`/`ServeRuleResolver`）仅在**甜蜜传递**改为规则级外来子技能（`DishInstance.TransferredSkills`、`SkillEffect` 载荷），其余零改动。
- 描述由 `SkillDescComposer` 回填占位符生成（占位符表见配置表说明）。
- 「视为N个食物」不再用 `dish_base.countAs` 承载：**所有 `dish_base.countAs` 恒为 1**，改由该技能自己的被动子技能（Self AddCountAs，补 N-1）表达。描述用 `{countas}=actionValue+1` 显示总数 N；运行时 `base 1 + (N-1) = N` 有效视为食物数不变。定向/条件「额外视为」仍为增量语义。

下列为效果目录（策划语义参考）。

## 主动技能
- 处在边缘时，倍率+3。
- 周围每有一种食物，分数+8。
- 甜蜜传递至同行1 个食物。
- 周围每有1个空格，分数-2
- 周围每有1 个食物，蛋糕+2层
- 本食物结算时1次，分数永久+6
- 同列每有一个食物，倍率+1
- 同行每有一个食物，分数+10
- 本食物每有1个技能，分数永久+5
- 每有1种食物，周围食物倍率+0.2
- 蛋糕+5层/蛋糕+10层（参数控制值）
- 甜蜜传递至1 个食物
- 如果周围被填满，获得30个金币
- 如果同行被填满，蛋糕+10层
- 临时复制本食物至空格中
- 甜蜜传递至同列2 个食物
- 周围占1格的食物时，倍率 ×1.5。
- 周围每有1 个食物，倍率 ×1.2。
- 甜蜜传递至周围2 个食物
- 获得周围食物的1个技能。
- 自身每有1个技能，使蛋糕+6层
- 倍率 ×1.5
- 分数永久x1.2
- 甜蜜传递至2 个食物
- 甜蜜传递至2个周围食物
- 每有1 个食物，倍率+0.2
- 周围食物倍率+5
- 甜蜜传递至3 个食物
- 自身每有1个技能，使蛋糕+6层
- 甜蜜传递至同行食物
- 每有1个技能，倍率 ×1.5。
- 获得3种不同的蛋糕技能
- 周围食物倍率 ×1.5
- 周围每有1种食物，蛋糕层数+2层
- 每有1个技能，倍率 ×1.5
- 同行同列带有甜蜜传递的食物，执行一次它们的甜蜜传递
- 同列食物倍率 ×1.4
- 甜蜜传递至周围食物
- 周围食物倍率 ×1.5
- 带有甜蜜传递的食物达「3/5/8」种，此类食物倍率x「1.5/2.5/5」
- 倍率 ×2
- 蛋糕数量达3/5/8时，蛋糕层数+10/20/40
- 同行每有1 个食物，周围食物倍率+0.3
- 倍率+5
- 获得3种不同的蛋糕技能
- 复制周围的两个技能
- 同列食物倍率 ×2
- 每有1个技能，倍率+5
- 甜蜜传递至同列食物
- 倍率+5
- 蛋糕层数+15
- 分数永久+10
- 将分数加到所有蛋糕上
- 倍率+4
- 同列食物额外被视为2 个食物

## 被动技能

- 技能额外触发1次
- 视为2 个食物/视为4 个食物/视为8 个食物/视为5 个食物/视为10 个食物
