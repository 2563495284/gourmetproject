# 实现参考

> 拆分自 [游戏设计总览](../游戏设计.md)，合并原文档「15. 推荐工程模块拆分」「16. 推荐核心状态结构」「17. 关键实现规则」。

## 推荐工程模块拆分

```text
GameRun
  - 管理单局生命周期状态
  - 保存经营方向、周数、天数、金币、装饰品和消耗品、食物、餐桌格和触发记录

ActionRandomService
ActionScheduleService
  - 生成并持久化整局行动组序列
  - 按周内已出现次数依次处理最小保底、最大保底与自由权重随机
  - 从当前行动组内生成 n 选一行动（最多 3 个）与行动选择快照

ActionRandomService
  - 提供组内候选筛选、前置条件、不可重复行动过滤和权重随机工具
  - 不再从全局行动池直接生成主循环行动选择

ActionExecutor
  - 执行行动结果
  - 推进天数
  - 修改局外状态

TimelineService
  - 初始化时间轴
  - 推进时间轴
  - 检测经过节点
  - 提供节点数据

EventService
  - 事件随机
  - 展示事件
  - 执行事件选项结果

BossService
  - 根据节点池、周和解锁条件筛选 Boss
  - 按权重随机 Boss

FoodChallengeService / BattleForm
  - 处理经营挑战
  - 判断分数是否达标
  - 发放奖励或触发失败

ShopService
  - 根据隐藏分刷新商品
  - 处理购买、编辑食谱、删除食物

RewardService / RewardGranter
  - 统一发放金币、食物、装饰品和消耗品、餐桌格等奖励

SettlementService
  - 生成胜负结算数据
  - 生成新解锁内容展示
```

## 推荐核心状态结构

```ts
type RunState = {
  runId: string;
  characterId: string;
  seedText: string;

  currentWeek: number;
  currentDay: number;
  timelineId: string;
  timelineLengthDays: number;
  actionStepIndex: number;
  runActionStepIndex: number;
  actionGroupSequence: string[];

  gold: number;
  requiredScoreOverride?: number;

  bonusDishIds: string[];
  stomachFragmentIds: string[];
  items: RunItemState[];

  triggeredNodeIds: string[];
  usedEventIds: string[];
  usedActionIds: string[];
  completedBossIds: string[];

  result?: "playing" | "win" | "lose";
};

type TimelineNode = {
  id: string;
  timelineId: string;
  day: number;
  nodeType: "boss" | "interest" | "shop" | "event";
  payloadValue?: number;
  payloadParam?: string;
};
```

## 关键实现规则

1. 文档和实现以“7 天游标 + 整局行动组序列 + 组内 n 选一（最多 3 个）”为主口径。
2. 行动组合序列按整局累计行动序号逐步生成；计数按周独立，先筛低于最小保底的大组，再筛低于最大保底的大组，否则按当周权重完全放回随机（允许连续同组）。
3. 行动随机必须过滤前置条件、不重复限制和后续扩展的经营方向/难度限制。
4. 行动执行后先推进天数，再检查经过的时间轴节点。
5. 如果一次行动经过多个节点，应按节点在时间轴上的顺序依次结算。
6. 经营挑战未达目标美味值，立即失败。
7. 普通 Boss 胜利，立即进入下一周。
8. 最终周 Boss 胜利，立即胜利。
9. 商店商品根据隐藏分和权重随机。
10. 删除食物需要扣除金币。
11. 结算时展示胜负、局内指标和新解锁内容。
