namespace GourmetProject.Game.Presentation.Battle
{
    public enum BattleScopeHighlightChannel
    {
        Persistent = 0,
        Flash = 1,
        Settlement = 2,
    }

    /// <summary>Scope 区域在同一视觉语言中的语义角色。</summary>
    internal enum BattleScopeRegionRole
    {
        Action = 0,
        Condition = 1,
    }
}
