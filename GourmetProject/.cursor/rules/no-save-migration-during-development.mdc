---
description: 开发阶段不做旧存档兼容和迁移
alwaysApply: true
---

# 开发阶段存档策略

项目处于开发阶段时，运行时状态结构可以直接按当前设计重建，不为旧存档添加兼容层或迁移分支。

- 修改 `RunSaveData`、`GameRun`、奖励、商店、菜谱、道具等状态结构时，以当前版本数据模型为准。
- 不要为了旧字段、旧语义或历史测试夹带兼容逻辑。
- 需要调整测试时，改成验证当前设计的行为，而不是保留旧存档期望。
- 真正发布后若需要存档兼容，再引入显式版本号和迁移流程。
