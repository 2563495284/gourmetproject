---
description: 战斗表现层的阴影与受光约定（用假阴影，不用 2D 光照实现阴影）
globs: Assets/GameMain/Scripts/Game/Gameplay/Presentation/**/*.cs
alwaysApply: false
---

# 战斗阴影 / 受光约定

阴影一律用「假阴影」（drop shadow），**禁止**用 URP 2D 光照（`Light2D` + `ShadowCaster2D`）实现真实投影。这是经过对比后定下的方向，不要回退。

## 为什么

- 玩法是俯视格子摆盘 + 固定视角，真实 2D 阴影的卖点（动态方向）用不上。
- 背包乱斗式「悬浮举高 / 落定贴桌」的高度感只能靠假阴影的偏移/缩放/透明度表现，`ShadowCaster2D` 做不出高度。
- 真实阴影要为异形 footprint 生成轮廓、密集摆放互相投影显脏，性价比低。

## 实现方式（现状，照此维护）

- 阴影 = 程序生成的径向羽化软暗斑（`BattleShadow.SoftShadowSprite` / `DiffuseShadowSprite`，在 `DishPieceView.ConfigureContactShadow` 里装配），缩放铺在菜品脚下，不依赖菜品图的透明留白。
- **高度 = 本体局部抬升，阴影钉地面**：`DishPieceView.SetLiftHeight(worldHeight)` 只把本体视觉枢轴（`VisualPivot`）沿世界 +Y 抬高，根节点保持贴格、接触阴影始终留在地面脚印中心。`SetLift(0..1)` 保留为映射到参考举高高度的便捷封装。
  - 切忌再用「移动整个根节点（连带阴影子物体）」来表现飞行高度——那会让阴影飞到半空、还在飞行层盖住沿途菜品。
- **双层阴影做高度虚化**：核心层（`SoftShadowSprite`，有实心核）贴桌锐利、随高度淡出；弥散光晕层（`DiffuseShadowSprite`，无实心核，挂在 `ShadowHalo` 子物体）贴桌隐形、随高度放大显现。离地越高 → 越大、越淡、越虚；落定 → 收紧变实。参数都在 `DishPieceView` 上 `[SerializeField]`（`_shadowLiftRefCells`/`_coreGrow`/`_coreFadeWhenHigh`/`_haloGrow`/`_haloAlphaWhenHigh`）。
- **分层只飞本体、阴影留地面**：`SetFlying(true)` 只把<b>本体</b>切到 `PiecesFlying` 压在已摆放菜品之上（这是需要的，因为飞行菜在上层）；阴影核心/光晕固定留在 `Pieces` 的 `OrderShadow`（光晕再低一档），排在所有菜本体之下，铺在桌面、被沿途菜品遮挡，不会盖出脏暗斑。

## 受光：全程 Unlit 平涂

战斗 sprite 一律走 `SpriteRenderStyle.ApplyUnlitMaterial`（`Sprite-Unlit-Default`），画面靠原色 + URP 2D 卡通后处理出味。

```csharp
// BAD：套 Lit 材质又依赖 Light2D 受光 —— 删灯/分层就全黑，且与「灯目标排序层」强耦合
renderer.sharedMaterial = SpriteLitMaterial; // Sprite-Lit-Default

// GOOD：Unlit 平涂，不依赖任何 Light2D
SpriteRenderStyle.ApplyUnlitMaterial(renderer);
```

- 不要为了「阴影 / 立体感」往战斗场景加 `Light2D`。`Light2D` 默认只照亮其 `targetSortingLayers`（场景预置灯常年只认 `Default`），一旦 sprite 分到命名层就会失光变黑。
- 渲染前后顺序用命名 Sorting Layer（见 `BattleSorting`），不要堆裸 `sortingOrder` magic number。
