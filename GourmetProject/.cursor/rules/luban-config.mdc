---
description: Luban 配置表的新增与生成流程
globs: GameConfig/**
alwaysApply: false
---

# Luban 配置

配置源在 `GameConfig/`：**schema（表登记/枚举/子结构）和数据全在 xlsx 里**，XML 基本不用碰。
**强制：数据表一律用 xlsx，禁止用 json 手写数据。** 运行时产物（`StreamingAssets/Config/*.json`）由 Luban 自动生成，属生成物，勿手改、勿提交当作源。

## schema 都在 Excel（`luban.conf` 已挂载）
三个定义文件在 `GameConfig/Datas/`，由 `luban.conf` 的 `schemaFiles` 以对应 `type` 挂载：
- `__tables__.xlsx`（`type:"table"`）：**表登记**。一行=一张表，列含 `full_name / value_type / read_schema_from_file(=True) / input / index / comment …`。注释写 `comment` 列。
- 枚举定义拆为多个 xlsx（`luban.conf` 中多条 `type:"enum"`），每文件单 sheet、只放一类枚举：
  - `__enums_tag.xlsx` — TagCategory、TagEffectType
  - `__enums_item.xlsx` — ItemKind、ItemQuality、ItemTriggerTiming
  - `__enums_reward.xlsx` — RewardKind、RewardPoolKind
  - `__enums_action.xlsx` — ActionType、TimelineNodeType
  被 C# `switch` 的枚举改名/改值会改动生成代码，谨慎。**勿**把枚举拆成同文件多 sheet，也勿用 openpyxl `append` 从零拼表（会破坏空单元格 `inlineStr`，导致 alias 列解析失败）。
- `__beans__.xlsx`（`type:"bean"`）：**子结构 bean**（被 `list,Bean` 引用、又不是独立表的，如 `RecipeEntry`，带 `sep=","`）。
- 字段结构：`read_schema_from_file=True` 时**直接读各数据 xlsx 表头的 `##var` + `##type` 两行**，不再单独写 bean。改字段=只改数据表表头。
- `Defines/` 现仅剩框架自带的 `builtin.xml`（vec2/vec3）；玩法 schema 不再写 XML。

## 新增一张表
1. 在 `GameConfig/Datas/xxx.xlsx` 放数据并写全表头。第 1 列单元格作行类型标记：
   - `##var` 行：字段名；数据行第 1 列留空。
   - `##type` 行：**必填**（结构来源）。类型如 `string`/`int`/`float`/`bool`/`list,string`/枚举名/`list,RecipeEntry`。
   - `##comment` / `##` 行：注释（会被带进生成代码的字段文档，建议写）。
   - 枚举值写名字字符串（如 `Inherent`）。
2. 在 `GameConfig/Datas/__tables__.xlsx` **加一行**登记（Luban 不会自动扫表，这一行省不掉）：
   填 `full_name=TbXxx`、`value_type=Xxx`、`read_schema_from_file=True`、`input=xxx.xlsx`、`index=id`、`comment=...`。
   - 多文件源：`input` 逗号分隔；指定 sheet：`Sheet1@xxx.xlsx`（**sheet 名在前、@ 后是文件**，sheet 的 A1 须以 `##` 开头才被当数据表）；单例表 `mode=one`，数据 xlsx 首列写 `##column` 走竖表。
   - 多表同文件：一个 xlsx 放多个 sheet（每 sheet 一张表），各表 `input` 各写 `表sheet名@文件.xlsx` 即可（如 `dish_base@dish.xlsx`、`dish_variant@dish.xlsx`）。
3. 新增枚举：在对应类别的 `Datas/__enums_*.xlsx` 加（表头三行 `##var`/`##var`/`##` + 子字段 `name/alias/value/comment/tags`，照现有行往下加）。新文件请从已能 `gen.sh` 通过的枚举 xlsx **复制后删行**，不要 openpyxl 从零创建。
4. 新增子结构 bean：在 `Datas/__beans__.xlsx` 加（含 `sep` 列与 `*fields` 子字段 `name/alias/type/...`，照现有行加）。
5. 多元素 list（本项目 Luban 版本已实测，务必遵守）：
   - 单格 `#sep=` / `(list#sep=…)` 与「横向流式多列」均**不生效**，不要用。
   - 多元素 list 用**多行模式**：字段名前加 `*`（如 `*shapeRows`、`*pool`）放在 ##var 行末（多行字段须是最后一列）。
     每个元素占一行，续行的所有前导字段列**留空**（靠 index 列是否为空区分记录）。
   - `list,Bean`：每个元素写在一个单元格里，靠该 bean 的 `sep=","` 切分字段（如 `__beans__.xlsx` 里的 `RecipeEntry`），单元格写 `rice,150,0`。
   - 仅含 0/1 个元素的 list 可直接用单格，无需多行。
   - 转换/校验可参考脚本 `GameConfig/Tools/json_to_xlsx.py`。
6. 生成：
   ```bash
   bash GameConfig/gen.sh
   ```

## 输出位置（勿手改）
- 代码 → `Assets/GameMain/Scripts/Config/Gen/`（每次生成会被清空重写）。
- 数据 → `Assets/StreamingAssets/Config/`（运行时按表名小写 `.json` 读取，由 Luban 生成）。

## 运行时加载
- 统一走 `GameApp.Config`（`ConfigService`）。访问示例：`GameApp.Config.Tables.TbXxx.Get(id)`。
- 加载入口在 `ProcedurePreload`，整局只加载一次。

## 红线
- 不要在 `GameConfig/Datas/` 放 `*.json` 源数据（已废弃，改用 xlsx）。
- 不要在 `Config/Gen/` 放手写代码（会丢失）；手写的加载/包装放 `Config/` 根目录。
