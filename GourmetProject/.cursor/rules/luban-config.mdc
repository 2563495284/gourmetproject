---
description: Luban 配置表的新增与生成流程
globs: GameConfig/**
alwaysApply: false
---

# Luban 配置

配置源在 `GameConfig/`：**表结构与数据都在 xlsx 里**，XML 只留极少量"被引用的类型"与表登记。
**强制：数据表一律用 xlsx，禁止用 json 手写数据。** 运行时产物（`StreamingAssets/Config/*.json`）由 Luban 自动生成，属生成物，勿手改、勿提交当作源。

## 结构来源（重要）
- 所有 `<table>` 均用 `readSchemaFromFile="true"`：**表结构直接读 xlsx 表头的 `##var` + `##type` 两行**，不再在 XML 写 `<bean>`。改字段=只改 xlsx 表头。
- 枚举集中在 `GameConfig/Datas/__enums__.xlsx`（在 `luban.conf` 以 `{"fileName":"Datas/__enums__.xlsx","type":"enum"}` 挂载），策划可自助增删枚举值。
- XML（`Defines/gourmet.xml` 等）现在只保留：① 表登记 `<table ... readSchemaFromFile="true"/>`；② 被 `list,Bean` 引用的**子结构 bean**（如 `RecipeEntry`，它不是独立表，必须在 XML 定义且带 `sep=","`）。

## 新增一张表
1. 在 `GameConfig/Datas/xxx.xlsx` 放数据并写全表头。第 1 列单元格作行类型标记：
   - `##var` 行：字段名；数据行第 1 列留空。
   - `##type` 行：**必填**（结构来源，已开 readSchemaFromFile）。类型如 `string`/`int`/`float`/`bool`/`list,string`/枚举名/`list,RecipeEntry`。
   - `##comment` / `##` 行：注释（会被带进生成代码的字段文档，建议写）。
   - 枚举值写名字字符串（如 `Inherent`）。
2. 在 `GameConfig/Defines/*.xml` 加一行表登记（这一行省不掉，Luban 不会自动扫表）：
   ```xml
   <table name="TbXxx" value="Xxx" index="id" input="xxx.xlsx" readSchemaFromFile="true"/>
   ```
   - 指定 sheet：`input="xxx.xlsx@Sheet1"`；单例表用 `mode="one"`，xlsx 首列写 `##column` 走竖表。
   - 若想连这行也搬进表：可改用 `Datas/__tables__.xlsx`（`type="table"` 挂载），加表=加一行。
3. 新增枚举：在 `Datas/__enums__.xlsx` 加（表头两行 `##var` + 子字段 `name/alias/value/comment/tags`，参见现有内容）。被 C# 代码 `switch` 的枚举改名/改值会影响生成代码，谨慎。
4. 多元素 list（本项目 Luban 版本已实测，务必遵守）：
   - 单格 `#sep=` / `(list#sep=…)` 与「横向流式多列」均**不生效**，不要用。
   - 多元素 list 用**多行模式**：字段名前加 `*`（如 `*shapeRows`、`*pool`）放在 ##var 行末（多行字段须是最后一列）。
     每个元素占一行，续行的所有前导字段列**留空**（靠 index 列是否为空区分记录）。
   - `list,Bean`：每个元素写在一个单元格里，靠 schema 给该 bean 加 `sep=","` 切分字段：
     `<bean name="RecipeEntry" sep=","> … </bean>`，单元格写 `rice,150,0`。
   - 仅含 0/1 个元素的 list 可直接用单格，无需多行。
   - 转换/校验可参考脚本 `GameConfig/Tools/json_to_xlsx.py`。
5. 生成：
   ```bash
   bash GameConfig/gen.sh
   ```

## 输出位置（勿手改）
- 代码 → `Assets/GameMain/Scripts/Config/Gen/`（每次生成会被清空重写）。
- 数据 → `Assets/StreamingAssets/Config/`（运行时按表名小写 `.json` 读取，由 Luban 生成）。

## 运行时加载
- 统一走 `GameApp.Config`（`ConfigService`）。访问示例：`GameApp.Config.Tables.TbXxx.Get(id)`。
- 加载入口在 `ProcedurePreload`，整局只加载一次。

## 红线
- 不要在 `GameConfig/Datas/` 放 `*.json` 源数据（已废弃，改用 xlsx）。
- 不要在 `Config/Gen/` 放手写代码（会丢失）；手写的加载/包装放 `Config/` 根目录。
