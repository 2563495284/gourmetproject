---
description: 表现层场景物体的固定结构用脚本根 prefab + Instantiate，只有数据驱动内容才代码生成
globs: Assets/GameMain/Scripts/**/Presentation/**/*.cs
alwaysApply: false
---

# 表现层：固定结构用脚本根 prefab

场景物体表现单元（棋盘格、菜品、世界按钮、菜单书等）的**固定结构必须以 prefab 资产存在**，由控制器 `[SerializeField]` 引用并 `Instantiate`。除「数据驱动的动态列表/显示」外，固定的都要走 prefab。

## 约定

- **脚本根 prefab**：prefab 里只挂逻辑脚本（如 `WorldButtonView`/`BoardCellView`/`MenuBookWorldView`），渲染体（`SpriteRenderer`/`TextMesh`/`Collider`）可由脚本运行时补齐——这是项目既定风格。
- **数据驱动才动态**：随数量/数据变化的内容（菜名列表、分页、动态按钮等）用代码生成填充，其余结构不要凭空 `new`。
- **新增 prefab 后**：在场景里给控制器对应 `[SerializeField] xxxPrefab` 字段赋值（优先用 MCP 改场景，避免手改 `.unity` YAML）。

## 示例

```csharp
// ❌ BAD：凭空创建固定结构
var go = new GameObject($"MenuBook_{i}", typeof(MenuBookWorldView));
MenuBookWorldView book = go.GetComponent<MenuBookWorldView>();

// ✅ GOOD：从脚本根 prefab 实例化，再喂数据
MenuBookWorldView book = Instantiate(_menuBookPrefab, transform);
book.gameObject.name = $"MenuBook_{i}";
book.SetData(dishIds, db); // 动态内容由数据驱动填充
```
