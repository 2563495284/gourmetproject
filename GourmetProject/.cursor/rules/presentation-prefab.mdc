---
description: 表现层场景物体的固定结构优先在 prefab 里预拼好（SerializeField 引用），运行时补齐仅作兜底，数据驱动内容填进命名容器
globs: Assets/GameMain/Scripts/**/Presentation/**/*.cs
alwaysApply: false
---

# 表现层：固定结构在 prefab 里预拼好

场景物体表现单元（棋盘格、菜品、世界按钮、菜单书、飘字、连线等）的**固定结构必须以 prefab 资产存在**，由控制器 `[SerializeField]` 引用并 `Instantiate`。除「数据驱动的动态列表/显示」外，固定的都要走 prefab。

## 约定

- **预拼优先**：渲染体（`SpriteRenderer`/`TextMesh`/`LineRenderer`/`Collider`）要**尽量在 prefab 里拼好**并用 `[SerializeField]` 引用，让设计师能在 Inspector 可视化调整（位置、尺寸、颜色、字号、线宽等）。脚本里写死的外观 `const`（颜色/间距/阴影 alpha/线宽等）应提升为 `[SerializeField]` 暴露出来。参考已迁移的 `BoardCellView`/`WorldButtonView`/`ServeHandView`/`DishPieceView`/`MenuBookWorldView`。
- **运行时补齐仅作兜底**：脚本保留 `EnsureRefs()`（或等价的「引用为空才解析/补齐」）容忍 prefab 未拼好的情况，保证不崩、可渐进迁移；这是兜底，不是常态，不要靠它替代预拼。
- **数据驱动才动态**：数量随运行数据变化的内容（菜名条目、分页、主动道具按钮等）才代码生成，且必须**填进 prefab 里预置的命名容器**（如 `MenuBook` 的 `LeftPage`/`RightPage`），不要直接挂根节点或凭空拼整个结构。其余固定结构不要 `new`/`Instantiate`。
- **尺寸随运行数据变 = 根缩放贴合**：表现单元整体尺寸随棋盘/布局变化时，prefab 按一个**参考尺寸**拼好，运行时对根 `localScale` 缩放贴合（见 `MenuBookWorldView` 的 `RefW`/`RefH`），而不是逐元素在代码里按比例摆位。
- **固定数量 = 固定结构**：HUD 上数量恒定的按钮/单例元素（如「吃!」「总览」「清空涂鸦」「显隐」），要**预置在场景**里、用 `[SerializeField]` 引用，运行时只 `Configure`（喂文案/颜色/回调），位置交给场景。别借用数据驱动按钮的运行时 `Instantiate` 套路去造它们。
- **新增/改 prefab**：优先用 Unity MCP 编辑 prefab 与场景（`assets-prefab-open` / `gameobject-*` / 改 `.unity`），避免手改 YAML；改完在场景里给控制器对应 `[SerializeField] xxxPrefab` 字段赋值。

## 示例

```csharp
// ❌ BAD：渲染体全靠运行时 new / AddComponent 拼，prefab 是空壳
private void BuildVisual()
{
    var bg = new GameObject("Bg"); bg.AddComponent<SpriteRenderer>();
    // ...一大段 new/AddComponent 拼整个结构
}

// ✅ GOOD：结构预拼在 prefab，脚本引用 + 喂数据；缺引用才兜底补齐
[SerializeField] private SpriteRenderer _bg;
[SerializeField] private Transform _leftPage; // 数据驱动条目的命名容器
private void Build(float worldWidth, float worldHeight)
{
    transform.localScale = new Vector3(worldWidth / RefW, worldHeight / RefH, 1f); // 根缩放贴合
    BindAuthored();           // 绑定 prefab 预拼结构、归一化材质/排序
    // 动态条目 Instantiate 到 _leftPage / _rightPage 命名容器
}
```

```csharp
// ❌ BAD：凭空创建固定结构
var go = new GameObject($"MenuBook_{i}", typeof(MenuBookWorldView));
MenuBookWorldView book = go.GetComponent<MenuBookWorldView>();

// ✅ GOOD：从 prefab 实例化，再喂数据
MenuBookWorldView book = Instantiate(_menuBookPrefab, transform);
book.gameObject.name = $"MenuBook_{i}";
book.SetData(dishIds, db); // 动态内容由数据驱动填充进预置容器
```

```csharp
// ❌ BAD：固定数量按钮（永远 2 个）用数据驱动那套运行时造，还在代码里摆位置
_clearButton = InstantiateWorldButton();
_clearButton.transform.position = new Vector3(_halfW - 0.9f, -_halfH + 0.45f, 0f);

// ✅ GOOD：场景预置 + SerializeField，运行时只喂外观/回调（位置在场景里摆好）
[SerializeField] private WorldButtonView _clearButton;
_clearButton?.Configure(size, "清空涂鸦", color, () => _doodle.Clear());
```
